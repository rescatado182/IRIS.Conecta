@using IRIS.UI.Models

<TablerForm Model="@MovilityRequestState.requirementsMovility">
<DataGrid>

    <DataGridItem Title="Requerimientos solicitados">
        <ItemSelect Items="enumRequirementsTypes"
                    @bind-SelectedValues="selectedRequirementTypes"
                    SelectedTextExpression="e => GetRequirementTypesDisplayName(e)"
                    MultiSelect>
            <ListTemplate>
                    @GetRequirementTypesDisplayName(@context)
            </ListTemplate>
        </ItemSelect>

    </DataGridItem>


</DataGrid>

<DataGrid>

    <DataGridItem Title="Fecha de inicio">
            <Datepicker @bind-SelectedDate="selectedInitialDate" />
    </DataGridItem>

    <DataGridItem Title="Fecha final">
            <Datepicker @bind-SelectedDate="selectedFinalDate" />
    </DataGridItem>


        <DataGridItem Title="Valor de la inscripción">
            <input type="text" class="form-control" @bind="MovilityRequestState.requirementsMovility.Total" />
            <ValidationMessage For="@(() => MovilityRequestState.requirementsMovility.Total)" class="text-danger" />
        </DataGridItem>
</DataGrid>

</TablerForm>

@*     <DataGridItem Title="Requerimientos solicitados">
        <Select Items="enumRequirementsTypes"
                @bind-SelectedValue="selectedRequirementTypes"
                TextExpression="e => Enum.GetName(typeof(EnumRequirementsTypes), e)"
                NoSelectedText="" />
    </DataGridItem> *@